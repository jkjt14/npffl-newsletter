# {{ title }} — Week {{ week }}

_Year {{ year }}, League {{ league_id }}_

## Standings Snapshot
| Team | PF | VP |
|---|---:|---:|
{% for r in standings_rows %}
| {{ r.name }} | {{ '%.1f'|format(r.pf) }} | {{ r.vp }} |
{% endfor %}

## Top Values (Pts per $1K)
{% for r in top_values %}
- **{{ r.name }}** ({{ r.pos }} {{ r.team }}) — {{ '%.1f'|format(r.pts) }} pts on ${{ '%.0f'|format(r.salary) }} → **{{ '%.2f'|format(r.ppk) }} pts/$1K**
{% endfor %}

## Biggest Busts (Costly Duds)
{% for r in top_busts %}
- **{{ r.name }}** ({{ r.pos }} {{ r.team }}) — {{ '%.1f'|format(r.pts) }} pts on ${{ '%.0f'|format(r.salary) }} → **{{ '%.2f'|format(r.ppk) }} pts/$1K**
{% endfor %}

## Lines to Watch
{% for o in odds %}
- **{{ o.matchup }}** — spread {{ o.spread }}, total {{ o.total }}
{% endfor %}

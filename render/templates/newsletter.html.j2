<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>NPFFL Weekly — Week {{ week.week }}</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial; margin: 24px; }
    h1 { margin: 0 0 8px; }
    h2 { margin-top: 28px; border-bottom: 1px solid #eee; padding-bottom: 4px; }
    .pill { display:inline-block;padding:2px 8px;border-radius:12px;background:#f3f4f6;font-size:12px;margin-left:8px;}
    .grid { display:grid; gap:10px; grid-template-columns: repeat(auto-fit, minmax(260px,1fr)); }
    .card { border:1px solid #eee; border-radius:12px; padding:12px; }
    ul { margin:8px 0 0 20px; }
    .muted { color:#666; font-size: 12px; }
  </style>
</head>
<body>
  <h1>NPFFL Weekly Newsletter <span class="pill">Week {{ week.week }}</span></h1>
  <div class="muted">Drop hits Tuesdays at {{ week.drop_time_et }} ({{ week.timezone }})</div>

  <h2>Quick Hits</h2>
  <ul>
    {% for line in nar.quick_hits %}<li>{{ line }}</li>{% endfor %}
  </ul>

  <h2>Weekly Results</h2>
  <div class="grid">
    {% for s in scores|sort(attribute="points", reverse=True) %}
      <div class="card">
        <strong>{{ teams_by_id[s.team_id] }}</strong><br/>
        {{ "%.2f"|format(s.points) }} pts
        {% if loop.first %} — 🔥 Top Dog{% endif %}
        {% if loop.last %} — 🗑️ Basement{% endif %}
      </div>
    {% endfor %}
  </div>

  <h2>Dumpster Fire of the Week</h2>
  <div class="card">
    <strong>{{ nar.dumpster_fire.name }}</strong> — {{ "%.2f"|format(nar.dumpster_fire.points) }} pts.<br/>
    {{ nar.dumpster_fire.line }}
  </div>

  {% if nar.fraud_watch %}
  <h2>Fraud Watch</h2>
  <div class="card">
    <strong>{{ nar.fraud_watch.name }}</strong> projects {{ "%.1f"|format(nar.fraud_watch.proj_next_week) }} next week.<br/>
    {{ nar.fraud_watch.line }}
  </div>
  {% endif %}

  {% if nar.vp_crime_scene %}
  <h2>VP Crime Scene</h2>
  <div class="card">
    <strong>{{ nar.vp_crime_scene.name }}</strong> missed 2.5 VP by {{ "%.2f"|format(nar.vp_crime_scene.diff) }}.<br/>
    {{ nar.vp_crime_scene.line }}
  </div>
  {% endif %}

  {% if nar.talk_spotlight %}
  <h2>Sh*t Talk Spotlight</h2>
  <div class="card">
    <strong>{{ nar.talk_spotlight.name }}</strong>: {{ nar.talk_spotlight.tag }}<br/>
    <ul>
      {% for b in nar.talk_spotlight.busts %}
        <li>Chalk bust: {{ b.player }} ({{ "%.1f"|format(b.pts) }} pts)</li>
      {% endfor %}
    </ul>
    <em>“{{ nar.talk_spotlight.quote }}”</em>
  </div>
  {% endif %}

  <div class="muted">Generated automatically — DFS blog vibe, league roast energy.</div>
</body>
</html>
